import 'package:flutter/material.dart';

class Weeklyscreen extends StatelessWidget {
  const Weeklyscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Halaman Weekly Tracker'),
    );
  }
}